{
  "name": "streamr-server",
  "version": "1.0.0",
  "description": "IPTV proxy server — resolves CORS for HLS streams",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev":   "node --watch server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@fastify/cors":        "^9.0.1",
    "@fastify/rate-limit":  "^9.1.0",
    "@fastify/reply-from":  "^9.7.0",
    "@fastify/static":      "^7.0.4",
    "fastify":              "^4.28.1",
    "undici":               "^6.19.2"
  }
}
